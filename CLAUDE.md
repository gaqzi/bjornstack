# Agent Instructions

## Issue Tracking

This project uses **bd (beads)** for issue tracking.
Run `bd prime` for workflow context.

Run `bd` commands outside the sandbox (`dangerouslyDisableSandbox: true`) — the
dolt server requires socket access that the sandbox blocks.
